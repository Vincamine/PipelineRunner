pipeline:
  name: sample-pipeline
  stages:
    - build
    - test
    - deploy

jobs:
  - name: compile
    stage: build
    script:
      - echo "Compiling source code"
  - name: unit-test
    stage: test
    needs:
      - compile
    script:
      - echo "Running unit tests"
  - name: deploy
    stage: deploy
    needs:
      - unit-test
    script:
      - echo "Deploying application"
