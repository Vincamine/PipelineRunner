pipeline:
  name: "DuplicateJobs"
  stages:
    - build
job:
  - name: "BuildJob"
    stage: "build"
    image: "openjdk:11"
    script:
      - "mvn clean install"
  - name: "BuildJob"  # Duplicate name
    stage: "build"
    image: "openjdk:11"
    script:
      - "echo Duplicate"
