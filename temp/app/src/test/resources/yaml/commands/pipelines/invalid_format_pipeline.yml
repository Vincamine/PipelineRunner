pipeline:
  name: InvalidFormat
  stages:
    - build
    - test
  jobs:  # Incorrect indentation and format
    - name: BuildJob
  stage: build  # Incorrect indentation
    image: maven:3.8.3-openjdk-17
      script:  # Invalid nesting
      - mvn clean compile

    - name: TestJob
        stage: test  # Inconsistent indentation
      needs:
        - BuildJob
      script:
        - mvn test
      image: maven:3.8.3-openjdk-17